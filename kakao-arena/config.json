{
    "tag": "dev",
    "description": "configuration for testing",
    "seed": 100,

    "mode": "dev",
    "checkpoint": "model.5.pth",
    "verbose": 1,
    "log_step": 100,

    "batch_size": 32,
    "num_workers": 16,
    "num_epochs": 10,
    "learning_rate": 1e-5,
    "momentum": 0.9,
    "weight_decay": 5e-3,

    "embedding_size": 100,
    "alpha": 100
}
